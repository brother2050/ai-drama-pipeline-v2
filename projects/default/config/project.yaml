# AI 短剧管线 v2 — 项目配置
# 系统级配置（ComfyUI、TTS、LLM 等）请编辑 config/system.yaml

project:
  name: "AI短剧"
  episodes: 1
  fps: 24
  resolution: [1280, 720]
  style: "cinematic"
  genre: "urban"

# 项目级覆盖（可选，取消注释可覆盖系统配置）
# comfyui:
#   url: "http://192.168.1.100:8188"
# llm:
#   enabled: true
