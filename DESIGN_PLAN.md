# 🎨 UI/UX 改造计划

> 参考: Seko 2.0 使用指南 + 当前项目现状分析
> 原则: **增强设计，不放弃原有架构**

---

## ✅ 已完成（第一轮）— UI/UX 基础改造

| # | 模块 | 改造内容 | 状态 |
|---|------|----------|------|
| 1 | 仪表盘 | Hero 区 + 统计卡片 + 快速入口网格 + 工具状态 | ✅ |
| 2 | 角色管理 | 表格 → 卡片画廊 + 空状态引导 | ✅ |
| 3 | 场景管理 | 表格 → 卡片画廊 + 空状态引导 | ✅ |
| 4 | 分镜表 | 新增时间轴视图 + 表格/时间轴切换 | ✅ |
| 5 | 生产管线 | 新增流程图（TTS→首帧→视频→口型→后期） | ✅ |
| 6 | 全局 | 自定义 modal 替代原生 prompt/confirm | ✅ |
| 7 | 全局 | 按钮加载态 + tooltip + 资源预览切换 | ✅ |
| 8 | 全局 | API Key 掩码 + 未保存提醒 + 状态徽章 | ✅ |

---

## 🔴 第二轮 — 核心体验提升（高优先级）

### 2.1 首页灵感输入（Seko 核心交互）

**现状**: 仪表盘只有导航按钮，用户不知道从哪开始
**目标**: 在仪表盘顶部增加「灵感输入框」，用户输入一句话即可启动全流程

**改动范围**:
- `app.js`: `loadDashboard()` 增加灵感输入框 + 生成按钮
- 点击「生成」→ 调用 `/llm/storyboard` → 跳转分镜表
- 支持「快速模式」（一键生成）和「高级模式」（可设置集数/时长/追加）
- ✅ **已完成** — 灵感输入框 + 高级选项 + dashInspireGen() + 进度条

### 2.2 角色/场景图片支持

**现状**: 角色卡片只显示文字描述首字母，无实际图片
**目标**: 支持上传/生成角色定妆照，卡片显示真实缩略图

**改动范围**:
- 后端: `POST /assets/{type}/{id}/upload` 图片上传端点 + `GET /assets/...` 访问端点
- 前端: 编辑面板增加图片上传区 + 拖拽上传
- 卡片: 优先显示定妆照，无图时回退到首字母占位
- 场景同理: 支持场景参考图上传
- ✅ **已完成** — 后端上传/访问 API + 前端拖拽上传 + 卡片缩略图

### 2.3 分镜表缩略图预览

**现状**: 时间轴视图中缩略图需要逐个异步加载，首次显示为 🎬 图标
**目标**: 首帧生成后自动显示缩略图，支持点击放大

**改动范围**:
- 时间轴卡片的 `timeline-thumb` 区域显示 frame.png
- 点击缩略图 → 资源预览浮层（已实现，需优化加载体验）
- 增加骨架屏/loading 占位
- ✅ **已完成** — 骨架屏 + 点击放大 + 加载失败回退

### 2.4 生产管线步骤状态联动

**现状**: 流程图步骤状态是静态的
**目标**: 执行任务时，流程图实时高亮当前步骤

**改动范围**:
- `runOne()` / `batchRun()` 执行时，更新 `pipeline-step` 的 class
- `active` → 脉冲动画 → `done` / `fail`
- 批量完成后，显示各步骤完成率
- ✅ **已完成** — _updatePipelineStep() + runOne/batchRun 联动 + 箭头联动

### 2.5 编辑面板增强

**现状**: 编辑面板字段平铺，长文本输入体验差
**目标**: 参考 Seko 的编辑体验

**改动范围**:
- 动作/台词输入框增加字数统计
- 角色/场景字段改为下拉选择（从已有配置中选）
- 增加「预览」按钮，直接在编辑面板中预览当前镜头资源
- 保存后自动跳到下一个镜头（连续编辑模式）
- ✅ **已完成** — 字数统计 + 角色/场景下拉 + 连续编辑（上/下一镜头）

---

## ✅ 已完成（第三轮部分 + 额外功能）

> 以下功能在 TODO.md 中标记为已完成，但原 DESIGN_PLAN 未覆盖

### 3.3 角色/场景与分镜联动 — 部分完成

- ✅ 角色/场景删除 API（`DELETE /api/{type}/{id}`）
- ✅ 角色/场景下拉选择（编辑分镜时从已有列表选取）
- ❌ 删除时引用计数提示（未实现）
- ❌ 角色卡片显示「被引用次数」（未实现）

### 分镜表内联编辑

- ✅ 表格视图直接编辑场景/角色/动作/台词/运镜/景别/时长
- ✅ 保存防抖（saveShot 500ms，批量 1000ms）
- ✅ 镜头删除功能（工作台 + 分镜表）

### 撤销/重做

- ✅ 历史栈 + `Ctrl+Z` / `Ctrl+Shift+Z` / `Ctrl+Y` 快捷键
- ✅ 工具栏 ↩/↪ 按钮
- ✅ 最大 50 步历史

### 批量执行优化

- ✅ 批量执行「取消」按钮（batchCancelled 标志）
- ✅ 批量完成摘要（成功/跳过/失败/取消）
- ✅ pollTask 最大 300 次轮询（约 4 分钟超时）

### 移动端响应式

- ✅ 侧边栏横滑（768px 断点）
- ✅ 卡片单列布局
- ✅ 编辑面板全屏
- ✅ 小屏适配（480px 断点）

### 前端工程化

- ✅ `cachedFetch` + `invalidateCache`（30s TTL 缓存层）
- ✅ `i18n.js` 国际化（中/英字典 + `t()` 函数 + 语言切换器）
- ✅ ESC 键关闭浮层/预览

### 后端加固

- ✅ Rate Limiting（滑动窗口 60s/120 次）
- ✅ 日志系统（6 模块 logger + 统一配置服务 + logs/app.log）
- ✅ 转场 xfade offset 精确计算 + acrossfade 音视频同步
- ✅ `-movflags +faststart` 流媒体优化

---

## 🟡 第三轮 — 效率与专业感（中优先级）

### 3.1 分镜表拖拽排序

**现状**: 镜头顺序固定，无法调整
**目标**: 支持拖拽调整镜头顺序

**改动范围**:
- 引入 SortableJS（CDN，~10KB）
- 时间轴视图和表格视图都支持拖拽
- 拖拽后自动保存

### 3.2 批量导入/导出

**现状**: 分镜只能手动添加或 AI 生成
**目标**: 支持 CSV/JSON 导入导出

**改动范围**:
- 分镜表工具栏增加「导入」「导出」按钮
- 导入: 解析 CSV → 校验 → 合并/覆盖
- 导出: 下载当前集分镜为 CSV

### 3.3 角色/场景与分镜联动

**现状**: 角色/场景配置与分镜表独立，编辑角色后需手动更新分镜
**目标**: 角色/场景变更自动同步到分镜表

**改动范围**:
- 编辑角色时，提示「N 个分镜引用了此角色，是否同步更新？」
- 删除角色时，提示引用数量
- 角色卡片显示「被引用次数」
- ⚠️ **部分完成** — 删除 API + 下拉选择已实现，引用计数/同步提示未做

### 3.4 配置预设模板

**现状**: 设置页需要手动填写所有字段
**目标**: 提供常用配置预设

**改动范围**:
- 设置页增加「快速配置」区
- 预设: 「本地 ComfyUI」「云端 SiliconFlow + MIMO」「Ollama 本地」
- 一键应用预设，自动填充所有字段

### 3.5 成片预览区

**现状**: 后期合成完成后，用户需要去文件系统找 final.mp4
**目标**: 管线页面底部增加「成片预览」区

**改动范围**:
- 检测 `output/e{ep}/episode_{ep}_final.mp4` 是否存在
- 存在时显示视频播放器 + 下载按钮
- 不存在时显示「尚未生成成片」

---

## 🟢 第四轮 — 高级功能（低优先级）

### 4.1 对话式编辑（Seko 核心特性）

**现状**: 所有编辑都通过表单
**目标**: 增加「对话式」编辑入口，用自然语言修改分镜

**实现思路**:
- 管线页面增加「💬 对话编辑」浮窗
- 用户输入: "把第 3 个镜头的台词改成..."
- 后端调用 LLM 解析意图 → 执行对应操作
- 返回操作结果 + 更新后的分镜

### 4.2 主体库管理（Seko 特性）

**现状**: 角色定妆照散落在各项目目录
**目标**: 全局主体库，跨项目复用角色

**改动范围**:
- 新增「主体库」页面（侧边栏新增入口）
- 全局存储: `shared_assets/characters/`
- 角色卡片支持「复制到当前项目」

### 4.3 多剧集管理

**现状**: 支持多集但 UI 上没有集数总览
**目标**: 项目级集数管理面板

**改动范围**:
- 项目管理页面增加「集数列表」
- 每集显示: 镜头数 / 时长 / 状态（未开始/进行中/已完成）
- 支持批量操作: 全集生成 / 全集导出

### 4.4 实时协作指示

**现状**: 单用户使用
**目标**: 显示当前是否有任务在执行

**改动范围**:
- 侧边栏底部显示「🔧 Worker 运行中 · 3 个任务」
- 导航项上显示角标（如有进行中的任务）

---

## ✅ 已完成（代码重构）— 消除冗余 + 结构优化

> 原则：提取工具函数，消除 if-null / 重复初始化 / 长 if-else 链
> 涉及文件：`app.js`、`api.py`、`tasks.py`

### 前端 — app.js 工具函数提取

| # | 改动 | 效果 | 状态 |
|---|------|------|------|
| J1 | `_editEntityPanel()` 通用编辑器 | 消除 editChar/editScene 编辑面板重复 | ✅ |
| J2 | `_loadEntityPage()` 通用列表渲染 | 消除 loadCharacters/loadScenes 卡片渲染重复 | ✅ |
| J3 | `_newEntityPanel()` 通用新建面板 | 消除 newChar/newScene 新建面板重复 | ✅ |
| — | `_html()` / `_btnLoad()` | 消除 if(el) el.innerHTML 冗余（19→0） | ✅ |
| — | `_runTool()` 通用工具执行 | 合并 runPortraits/runPost/runSubtitle | ✅ |
| — | `_runAIGen()` 通用 AI 生成 | 合并 doAIGenCharacter/doAIGenScene | ✅ |
| — | `_uploadImg()` + `_handleImgDrop()` | 合并 ec/es 图片上传 | ✅ |

**净减 ~200 行重复代码**

### 后端 — api.py 校验工具提取

| # | 改动 | 效果 | 状态 |
|---|------|------|------|
| A1 | `_proj()` + mtime 缓存 | 避免每次请求读文件 | ✅ |
| A2 | `_parse_entity()` | 统一 Pydantic → dict 转换 | ✅ |
| — | `_check_id/_check_uuid/_check_filename/_check_episode/_check_entity_type` | 消除 12 处 re.match 重复 | ✅ |
| — | `_active_project_dir()` → `_proj()` | 别名统一 | ✅ |

### 后端 — tasks.py 初始化统一

| # | 改动 | 效果 | 状态 |
|---|------|------|------|
| T1 | ai_characters_task / ai_scenes_task | 用 `_init_ctx` 替换 6 行样板 | ✅ |
| T2 | music_task | 用 `_init_ctx` 替换重复初始化 | ✅ |
| T3 | tts_single_task | 用 `_init_ctx` 替换重复初始化 | ✅ |
| T4 | `_run_subtitle` | 用 `_cfg_dir` 替换路径构建 | ✅ |
| T5 | `_shot_dir` | 用 `_cfg_dir` 替换路径构建 | ✅ |
| T6 | `_load_episode_shots()` | 统一 preview/produce 加载逻辑 | ✅ |

**净减 ~50 行重复代码，19 项重构全部完成**

---

## 📐 设计规范

### 色彩系统（已有，保持一致）

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 | `#6c8cff` | 按钮、链接、高亮 |
| 成功 | `#34d399` | 完成状态 |
| 错误 | `#f87171` | 失败状态 |
| 警告 | `#fbbf24` | 进行中/待处理 |
| 辅助 | `#a78bfa` | AI 相关功能 |

### 组件规范

| 组件 | 用途 | 优先级 |
|------|------|--------|
| `.entity-card` | 角色/场景卡片 | 高 |
| `.timeline-item` | 分镜时间轴项 | 高 |
| `.pipeline-step` | 管线流程步骤 | 高 |
| `.stat-card` | 统计数据卡片 | 中 |
| `.quick-entry` | 快速入口卡片 | 中 |
| `.empty-state` | 空状态引导 | 中 |
| `.view-toggle` | 视图切换按钮组 | 低 |

### 交互规范

| 场景 | 行为 |
|------|------|
| 删除操作 | `modalConfirm` 二次确认 |
| 文本输入 | `modalPrompt` 自定义弹窗 |
| 长文本 | 截断 + tooltip（30 字） |
| 加载中 | 按钮禁用 + `⏳ ...` |
| 操作成功 | 绿色 toast 3.5s |
| 操作失败 | 红色 toast 3.5s |
| 未保存离开 | `modalConfirm` 确认 |

---

## 📊 进度追踪

| 轮次 | 计划项数 | 已完成 | 剩余 |
|------|----------|--------|------|
| 第一轮（基础修复） | 8 | 8 | 0 |
| 第二轮（核心体验） | 5 | 5 | 0 |
| 代码重构（消除冗余） | 19 | 19 | 0 |
| 额外功能（内联编辑/撤销/i18n/缓存等） | 10 | 10 | 0 |
| 第三轮（效率提升） | 5 | 1 | 4 |
| 第四轮（高级功能） | 4 | 0 | 4 |
| **合计** | **51** | **43** | **8** |
