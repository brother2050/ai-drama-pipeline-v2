# TODO — 第二轮自检

> 2026-05-26 深度逻辑/功能缺陷检查

---

## 🟡 中优先级

### 1. 对话编辑无防重入 ✅ 已修复
- **问题**: `sendChatMsg()` 可连续点击，并发执行多个 LLM 任务
- **修复**: 添加 `_chatSending` 锁，发送中禁用按钮

### 2. 导入分镜使用过时的全局 shots ✅ 已修复
- **问题**: `doImport()` 合并模式使用内存中的 `shots`（可能过时），而非从 API 重新获取
- **修复**: 合并前先 `await api('/storyboard/${ep}')` 获取最新数据

### 3. 主体库复制不写数据库 ✅ 已修复
- **问题**: `copy_asset_to_project` 和 `add_to_shared_library` 只复制 YAML 文件，不写数据库
- **修复**: 复制后调用 db upsert 同步

### 4. 对话历史无限增长 ✅ 已修复
- **问题**: `_chatHistory` 只增不减，长对话占用越来越多内存
- **修复**: 限制最大 100 条，超出时移除最早的

## 🟢 低优先级 — 不修

### 5. get_storyboard 无文件锁
- **问题**: save 有锁但 get 没有，并发读写可能读到半写状态
- **影响**: uvicorn 单线程同步模式下不会发生。多 worker 部署才需要处理
- **状态**: 不修

### 6. get_final_resources 未用 _safe_path
- **问题**: 文件名来自 `.name` 属性，未做路径遍历检查
- **影响**: `.name` 是 `Path.name`（纯文件名，无路径分隔符），实际无风险
- **状态**: 不修
